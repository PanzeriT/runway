{{ define "dashboard.go" }}
{{ template "warning.go" . }}
package admin

import (
	"net/http"

	"github.com/labstack/echo/v4"
)

func dashboardHandler(c echo.Context) error {
	// user := c.Get("user").(*jwt.Token)
	// claims := user.Claims.(*jwtCustomClaims)
	// name := claims.Name

	return c.String(http.StatusOK, "Dashboard page should be here.")
}
{{ end }}
